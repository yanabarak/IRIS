<template>
  <pre class="neutral-color">
    - ОСНОВНІ КОМАНДИ
    - ОСНОВНІ ОПЦІЇ
    - СТВОРЕННЯ ТА НАЛАШТУВАННЯ FIREDASE HOSTING
    - РОЗГОРТАННЯ ПРОЕКТУ
    - ВИДАЛЕННЯ ПРОЕКТУ
    - ТАРИФНИЙ ПЛАН Blaze (Pay-as-you-go)








    ОСНОВНІ КОМАНДИ
    ************************************
    ####  **1. Встановлення Firebase CLI**
    ```bash
    npm install -g firebase-tools
    ```
    Переконайся, що **Node.js** встановлений (версія 18, 20 або 22).


    ####  **2. Вхід у Firebase**
    ```bash
    firebase login
    ```
    Авторизує користувача у Firebase через браузер.

    ```bash
    firebase logout
    ```
    Вихід із Firebase.


    #### **3. Ініціалізація проєкту**
    ```bash
    firebase init
    ```
    Запускає майстер налаштування Firebase для створення або підключення проєкту.


    #### **4. Деплой (розгортання)**
    ```bash
    firebase deploy
    ```
    Завантажує твій застосунок на Firebase Hosting або інші сервіси.

    ```bash
    firebase deploy --only hosting
    ```
    Розгортання **лише** на хостинг без інших сервісів.


    #### **5. Запуск локального сервера**
    ```bash
    firebase serve
    ```
    Запускає **локальний сервер** для тестування (застаріле, краще використовувати `firebase emulators:start`).


    #### **6. Емулятори Firebase**
    ```bash
    firebase emulators:start
    ```
    Запускає локальні емулятори для Firestore, Functions, Auth тощо.

    ```bash
    firebase emulators:export ./backup
    ```
    Зберігає дані з емуляторів у папку `backup`.

    ```bash
    firebase emulators:import ./backup
    ```
    Відновлює дані в емуляторах із резервної копії.


    ####  **7. Управління базою даних Firestore**
    ```bash
    firebase firestore:delete /users --recursive
    ```
    Видаляє колекцію `users` у Firestore.

    ```bash
    firebase firestore:indexes
    ```
    Перегляд індексів Firestore.


    #### **8. Управління хостингом**
    ```bash
    firebase hosting:disable
    ```
    Вимикає сайт на Firebase Hosting.

    ```bash
    firebase hosting:channel:create test
    ```
    Створює **попередній перегляд** для тестування перед основним деплоєм.


    ####  **9. Перегляд логів функцій**
    ```bash
    firebase functions:log
    ```
    Показує логи хмарних функцій Firebase.


    #### **10. Оновити токен**
    ```bash
    firebase login --reauth
    ```

    ---

    ### **Підсумок**
    🔹 **Ініціалізація** → `firebase init`
    🔹 **Логін** → `firebase login`
    🔹 **Розгортання** → `firebase deploy`
    🔹 **Локальне тестування** → `firebase emulators:start`
    🔹 **Керування Firestore** → `firebase firestore:delete`
    🔹 **Логи функцій** → `firebase functions:log`



















    ОСНОВНІ ОПЦІЇ
    *************************************************
    ◯ Hosting: Configure files for Firebase Hosting and (optionally) set up GitHub Action
    deploys
    ◯ Storage: Configure a security rules file for Cloud Storage
    ❯◯ Emulators: Set up local emulators for Firebase products
    ◯ Remote Config: Configure a template file for Remote Config
    ◯ Extensions: Set up an empty Extensions manifest
    ◯ Realtime Database: Configure a security rules file for Realtime Database and


     **Hosting** – якщо плануєте розміщувати веб-версію (або серверну частину) на Firebase.
     **Storage** – якщо потрібно зберігати документи (PDF, DOCX) у Firebase Storage.
     **Realtime Database** – якщо потрібно зберігати дані проаналізованих документів у реальному часі.
     **Emulators** – корисно для локальної розробки без підключення до Firebase (рекомендується для тестування).









    СТВОРЕННЯ ТА НАЛАШТУВАННЯ FIREDASE HOSTING
    *************************************************
    1. **Створити проєкт у Firebase:**
       - Перейди до [Firebase Console](https://console.firebase.google.com/).
       - Натисни "Додати проєкт" та введи його назву.
       - Вимкни "Google Analytics" (якщо не потрібен) і створи проєкт.


    2. **Встанови Firebase CLI:**
       Вимоги: має бути встановлений Node.js нова версія (разом із npm)
       ```bash
       sudo apt install nodejs npm
       ```

       ```bash
       sudo npm install -g firebase-tools
       ```

       Перевірка, чи працює:
       ```bash
       firebase --version
       ```


    3. **Увійди до Firebase:**
       ```bash
       firebase login
       ```
       Авторизуйся у браузері через Google-акаунт.


    4. **Ініціалізуй Firebase Hosting у локальному проєкті:**
       - Створи папку для файлів:
         ```bash
         mkdir firebase-blocklist && cd firebase-blocklist
         ```
       - Запусти ініціалізацію:
         ```bash
         firebase init hosting
         ```
       - Вибери проєкт, створений у Firebase Console.
       - Вкажи `public/` як папку для файлів.
       - Відмовся від SPA (односторінкового застосунку).












    РОЗГОРТАННЯ ПРОЕКТУ
    *************************************************
    #### **1. Створення та налаштування Firebase Hosting**

    1. **Створити проєкт у Firebase:**
       - Перейди до [Firebase Console](https://console.firebase.google.com/).
       - Натисни "Додати проєкт" та введи його назву.
       - Вимкни "Google Analytics" (якщо не потрібен) і створи проєкт.


    2. **Встанови Firebase CLI:**
       Вимоги: має бути встановлений Node.js нова версія (разом із npm)
       ```bash
       sudo apt install nodejs npm
       ```

       ```bash
       sudo npm install -g firebase-tools
       ```

       Перевірка, чи працює:
       ```bash
       firebase --version
       ```


    3. **Увійди до Firebase:**
       ```bash
       firebase login
       ```
       Авторизуйся у браузері через Google-акаунт.


    4. **Ініціалізуй Firebase Hosting у локальному проєкті:**
       - Створи папку для файлів:
         ```bash
         mkdir firebase-blocklist && cd firebase-blocklist
         ```
       - Запусти ініціалізацію:
         ```bash
         firebase init hosting
         ```
       - Вибери проєкт, створений у Firebase Console.
       - Вкажи `public/` як папку для файлів.
       - Відмовся від SPA (односторінкового застосунку).




    #### **2. Додавання файлів**

    1. У `public/` створи файл `file.json`:



    2. **Розгорни список на Firebase:**
       ```bash
       firebase deploy
       ```
       Це надасть тобі URL, щось на кшталт:
       ```
       https://your-project-id.web.app/file.json
       ```


















    ВИДАЛЕННЯ ПРОЕКТУ
    ************************************************
    ### 1. **Увійдіть до Firebase Console**
       - Перейдіть на [Firebase Console](https://console.firebase.google.com/).
       - Виберіть проєкт, який потрібно видалити.

    ### 2. **Відкрийте Налаштування проєкту**
       - У лівому меню натисніть на ⚙️ **Налаштування проєкту** (Project settings).

    ### 3. **Прокрутіть вниз до «Видалення проєкту»**
       - У вкладці **Загальні** (General) знайдіть розділ **Видалення проєкту** (Delete project).
       - Натисніть кнопку **Видалити проєкт**.

    ### 4. **Підтвердіть видалення**
       - Введіть назву проєкту для підтвердження.
       - Дотримуйтесь інструкцій, щоб остаточно видалити проєкт.

    ⚠ **Зверніть увагу:**
    - Видалення **незворотне** – усі дані, включаючи базу Firestore, Realtime Database, Storage, Authentication та інші сервіси Firebase, будуть втрачені.
    - Якщо у вас підключено **Google Cloud**, можливо, доведеться окремо видалити проєкт у [Google Cloud Console](https://console.cloud.google.com/).

    Якщо вам потрібно лише **тимчасово зупинити** використання Firebase (без повного видалення), розгляньте можливість **відключення платних функцій** або видалення окремих сервісів.










    ТАРИФНИЙ ПЛАН Blaze (Pay-as-you-go)
    ***********************************
    Тарифний план **Blaze (Pay-as-you-go)** у Firebase дозволяє оплачувати лише ті ресурси, які ви фактично використовуєте, без фіксованої щомісячної плати. Цей план включає безкоштовні ліміти, аналогічні до плану Spark, і ви сплачуєте лише за використання, що перевищує ці ліміти.

    **Основні безкоштовні ліміти та тарифи при перевищенні:**

    - **Cloud Firestore:**
      - **Зберігання даних:** 1 ГБ безкоштовно; понад це — **$0.18 за ГБ**.
      - **Записи:** 20 000 на день безкоштовно; понад це — **$0.18 за 100 000 операцій**.
      - **Читання:** 50 000 на день безкоштовно; понад це — **$0.06 за 100 000 операцій**.
      - **Видалення:** 20 000 на день безкоштовно; понад це — **$0.02 за 100 000 операцій**.

    - **Firebase Hosting:**
      - **Зберігання:** 10 ГБ безкоштовно; понад це — **$0.026 за ГБ**.
      - **Передача даних:** 10 ГБ на місяць безкоштовно; понад це — **$0.15 за ГБ**.

    - **Аутентифікація:**
      - **Перші 10 000 автентифікацій на місяць** — безкоштовно.
      - **Понад 10 000 на місяць:**
        - США, Канада, Індія: **$0.01 за автентифікацію**.
        - Інші країни: **$0.06 за автентифікацію**.

    - **Cloud Functions:**
      - **Виклики:** перші 2 мільйони на місяць — безкоштовно; понад це — **$0.40 за мільйон викликів**.
      - **Час виконання (GB-секунди):** перші 400 000 на місяць — безкоштовно; понад це — тарифи згідно з [Google Cloud Pricing](https://cloud.google.com/functions/pricing).

    Важливо зазначити, що навіть на плані Blaze, якщо ваше використання залишається в межах безкоштовних лімітів, ви не будете отримувати рахунки. Це корисно для розробників, які хочуть використовувати додаткові функції, доступні лише на плані Blaze, без негайних витрат. Однак, для запобігання несподіваним витратам, рекомендується налаштувати бюджети та сповіщення у консолі Firebase.

    Для детального розрахунку потенційних витрат ви можете скористатися [калькулятором цін Firebase](https://firebase.google.com/pricing).

    *Примітка: Ціни можуть змінюватися з часом. Рекомендується перевіряти актуальну інформацію на [офіційному сайті Firebase](https://firebase.google.com/pricing).*

  </pre>
</template>
